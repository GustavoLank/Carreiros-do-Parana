{
  "name": "carreirosdoparana",
  "private": true,
  "scripts": {
    "dev": "concurrently \"pnpm --filter web dev\" \"pnpm --filter api dev\"",
    "docker:up": "docker compose up -d postgres nginx",
    "docker:down": "docker compose down",
    "prisma:migrate": "prisma migrate dev",
    "prisma:generate": "prisma generate"
  },
  "devDependencies": {
    "@types/node": "^25.6.2",
    "concurrently": "^9.0.0",
    "dotenv": "^17.4.2",
    "prisma": "^7.8.0",
    "typescript": "^6.0.3"
  }
}
